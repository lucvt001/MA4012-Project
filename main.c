#include "main.h"

task main()
{
  // TODO: Add while block to detect when limit switch is pressed to start the robot

	while (1)
	{
		loop();
	}
}

// Main loop function. Add all main code in this function.
void loop()
{
  return;
}
